from Math.TransferProcess import draw_transfer_process
from Math.Roots import draw_root_picture

if __name__ == "__main__":
    # draw_transfer_process([2, 0, 0], [1, 1, 1], 10)
    draw_root_picture([1, 2, 1], [1, 0, 1])
